import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div className="min-h-screen bg-gray-50 flex items-center justify-center">
      <div className="text-center">
        <h1 className="text-4xl font-bold text-gray-900 mb-4">ddLOG</h1>
        <p className="text-gray-600">Gerenciador de Tarefas Diárias</p>
        <p className="text-sm text-gray-500 mt-2">Setup inicial completo. Aguarde implementação das funcionalidades.</p>
      </div>
    </div>
  </React.StrictMode>,
)